// Javascript Notes - Objects Simple

// Declaring an simple object
let person = {
    name: 'Lim Qi Wei',
    gender: 'male',
    age: 24
}

// Accessing objects
log(`
== Accessing Objects ==
Name : ${person.name}
Gender : ${person.gender}
Age : ${person.age}
=================
`)

// Shorter logging method
function log(message) {
    console.log(message);
}
